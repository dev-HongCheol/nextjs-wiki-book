import spaces from "./spaces";
import fontSizes from "./fontSizes";
import letterSpacings from "./letterSpactings";
import lineHeights from "./lineHeights";
import colors from "./colors";

export const theme = {
  spaces,
  fontSizes,
  letterSpacings,
  lineHeights,
  colors,
};
